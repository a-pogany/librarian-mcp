# MCP Server Configuration
# Note: docs.root_path is configured in config.json only (not via env)
MCP_HOST=127.0.0.1
MCP_PORT=3001

# Search Configuration
# Mode: keyword, semantic, or hybrid (default: hybrid)
SEARCH_MODE=hybrid

# RAG/Embeddings Configuration
# Enable embeddings for semantic search (default: true)
ENABLE_EMBEDDINGS=true

# Logging
LOG_LEVEL=info
